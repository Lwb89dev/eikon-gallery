<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Media access only. Deliberately absent: INTERNET (eikon has no network code in this phase
        and the OS enforces that), broad storage permissions, location services.
        See docs/PRIVACY.md.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Android 14+: lets the user grant access to a hand-picked subset. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- Requested only when the user asks to see a photo's location: Android redacts GPS otherwise. -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!--
        WAKE_LOCK, ACCESS_NETWORK_STATE, FOREGROUND_SERVICE and RECEIVE_BOOT_COMPLETED are added by WorkManager (and
        Media3), which runs the background analysis and resumes it after a reboot. None of them allows network
        access; only INTERNET would. It is absent from the `standard` build (the build fails if it appears) and
        declared only by the `backup` build (src/backup/AndroidManifest.xml).
    -->

    <application
        android:name=".EikonApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        tools:ignore="UnusedAttribute"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eikon.Starting">

        <!-- WorkManager is initialized by EikonApplication so that workers can be injected with Hilt. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
            Hands another app the temporary picture of an edit that is being shared, and nothing else: it is not exported and only
            grants the one file named in each share (the folder is cache/shared, see SharedFiles).
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.shared"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/shared_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                "Open with" for pictures: any app may ask eikon to show one picture it hands over (a content address). eikon reads only that
                picture, and only to show it. Videos are not offered.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
    </application>

</manifest>
